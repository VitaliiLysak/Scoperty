import { Directive } from '@angular/core';

@Directive({
  // tslint:disable-next-line:directive-selector
  selector: 'ng-container[app-base-modal-message]',
})
export class BaseModalMessageDirective {
}
